# First Python Program

This is my first Python assignment for Computer Science.

For this assignment, I learned how to:

- Use `print()` to display text
- Use `input()` to get information from the user
- Store information in variables
- Work with strings
- Combine strings and variables
- Run Python from the VS Code terminal
- Use Git to commit my work
- Publish a project to GitHub
- 
## Assignment 1.1 - Variables

In this assignment, I practiced creating variables and using them to store information.

I learned how to:

- Create variables
- Store strings in variables
- Use `print()` to display the values stored in variables
- Stage, commit, and sync changes using Git and GitHub

## Assignment 1.2 - Python Data Types

In this assignment, I learned about different types of data that can be stored in Python variables.

I learned how to:

- Use `str`, `int`, `float`, and `bool` to store different types of information
- Use `type()` to identify the data type stored in a variable
- Use `print()` to display different types of values

## My Progress

It's been a little while since I've worked with coding, but I'm glad to be getting back into it and learning again. 
