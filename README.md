Working through my first assignment in CS 
Learned to work with:
print() → output
input() → user input
"text" → strings
name = ... → variables/assignment
"text" + variable → string concatenation 
Little rusty but glad to work through learning again!
